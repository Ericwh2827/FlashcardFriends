<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.flashcardfriends.eric.flashcardfriends">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Create" />
        <activity android:name=".Edit" />
        <activity android:name=".AddNewCard" />
        <activity android:name=".SetList" />
        <activity android:name=".EditCard" />
        <activity android:name=".Play" />
        <activity android:name=".PlayOptions" />
        <activity android:name=".PracticeFlashcards" />
        <activity android:name=".TestFlashcards" />
        <activity android:name=".EditSetName" />
        <activity android:name=".NoSetsCreated" />
        <activity android:name=".NoCardsCreated"></activity>
    </application>

</manifest>